
import java.util.ArrayList;

public class Sim {
    
    public ArrayList<Player> order;

    public Sim(ArrayList<Player> order) {
        this.order = order;
    }

    public void chooseStructurePlacement(ArrayList<Player> order) {
        ArrayList<int[]> clickedCoords = new ArrayList<>();

        for (Player player : order) {
            // display buttons
            // wait until button clicked

            // if one of the vertices is clicked:
                // next player's turn
            
        }

    }
}
